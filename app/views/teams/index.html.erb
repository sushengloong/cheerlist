<div class="content_pane">
  <h1>All English Premier League Teams</h1>
  
  <table>
  
  <% if @teams.blank? %>
  <tr><td>No team is available</td</tr>
  <% end %>
  
  <% @teams.each do |team| %>
  <tr>
      <td><%= image_tag team.image_url.to_s, :alt => team.name %></td>
      <td><%= link_to team.name, team %></td>
    </tr>
  <% end %>
</table>
</div>